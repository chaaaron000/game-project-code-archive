using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class level1_bgm : MonoBehaviour
{
    // Start is called before the first frame update
    //엠비언트 사운드 깔리는 느낌 보고싶어서 임시로 만듬, 
    //레벨 1에 대한걸(다음 씬으로 넘어가는 기능이라던가, 레벨1bgm관리 라던가) 관리하는 스크립트에 넣으면 될듯
    void Start()
    {
        // Debug.Log("이거 왜 안나옴?");
        // SoundManager.Instance.BGM_Play("dark_ambient");
    }
}
